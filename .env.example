# Supabase — copie para .env.local e preencha com as credenciais do seu projeto.
# Sem estas variáveis o app roda em modo local (dados no navegador / localStorage),
# o que permite testar todo o fluxo sem backend.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
